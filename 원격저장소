remote

원격저장소:인터넷이나 네트워크 어딘가에 있는 서버에 있는 git 저장소
지역저장소:local machine에 저장되어 있는 git 저장소

git서버를 직접 설치 or git호스팅 서비스 이용

git remote add <원격저장소 이름> <원격저장소 주소>
git remote:현재 지역저장소에 추가되어 있는 원격저장소 목록을 출력(자세한 정보는 -v)

git push <원격저장소 이름> <branch이름>

git clone <원격저장소 주소> *해당 원격저장소는 'origin'이라는 이름으로 자동 추가됨

gitpull=git fetch(가져와서) + git merge(병합)

이미 push한 commit은 수정/삭제하지 말 것
